import { OptionalStringMap } from "../util";
import { Client } from "../client";
import { HttpResponse } from "../agent";
export interface Options {
    resource: string;
    action?: string;
    client: Client;
}
export interface Request {
    query?: OptionalStringMap;
    header?: OptionalStringMap;
    timeout?: number;
}
export interface Response<U> extends HttpResponse {
    body: U;
}
export declare const retrieveMethod: <T extends Request, U>(options: Options) => (id: string, request: T) => Promise<Response<U>>;
export declare const listMethod: <T extends Request, U>(options: Options) => (request: T) => Promise<Response<U>>;
